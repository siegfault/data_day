<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.2/jquery.min.js"></script>

    <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css">

    <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css">

    <!-- Optional theme -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap-theme.min.css">

    <!-- Latest compiled and minified JavaScript -->
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/js/bootstrap.min.js"></script>

    <title>DataDay</title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag "//www.google.com/jsapi", "chartkick" %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <nav class="navbar navbar-default navbar-static-top">
      <div class="container-fluid">
        <div class="navbar-header">
          <%= link_to(actions_path, class: 'navbar-brand') do %>
            <img src="<%= asset_path('data_day.png') %>">
          <% end %>

          <ul class="nav navbar-nav">
            <li>
              <h3 class='text-primary'>DataDay</h3>
            </li>
            <li>
              <h3 class='tag-line'>
                <small>
		  Day-to-day Reporting!
                </small>
              </h3>
            </li>
          </ul>
        </div>

        <div class="collapse navbar-collapse">
          <p class="navbar-text navbar-right">
            <% if session[:user_id] %>
              Signed in as <%= current_user.username %>
              <%= link_to 'Logout', sessions_logout_path, class: 'btn btn-default' %>
            <% else %>
              <%= link_to 'Login', sessions_login_path, class: 'btn btn-default' %>
              <%= link_to 'Sign Up', new_user_path, class: 'btn btn-default' %>
            <% end %>
          </p>
        </div>
      </div>
    </nav>

    <div class="alert alert-info" role="alert">
      <% if current_user.current_action %>
	Currently Activity: <span class='label label-default'><%= current_user.current_action.name %></span>
	<p class='pull-right'>
	  Hit <code>b+(keybind)</code> to change your action or <code>e</code> to end your current action.
	</p>
      <% else %>
        No current action. Hit <code>b</code> and then a keybind to begin an action.
      <% end %>
    </div>

    <%= tabs %>

    <div>
      <p><%= flash[:notice] %></p>
    </div>

    <%= yield %>

  </body>
</html>
